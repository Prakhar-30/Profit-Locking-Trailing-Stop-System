[profile.default]
src = "src"
out = "out"
libs = ["lib"]
solc_version = "0.8.20"

[rpc_endpoints]
sepolia = "${SEPOLIA_RPC}"
reactive = "${REACTIVE_RPC}"
base = "${BASE_RPC}"

[etherscan]
sepolia = { key = "${ETHERSCAN_API_KEY}" }
